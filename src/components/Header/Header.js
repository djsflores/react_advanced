import React from 'react';
import { Button } from '../Button';

const Header = () => {
  return (
    <header>
      <h2>Header</h2>
      <input type="text" />
      <Button />
    </header>
  );
};

export default Header;
